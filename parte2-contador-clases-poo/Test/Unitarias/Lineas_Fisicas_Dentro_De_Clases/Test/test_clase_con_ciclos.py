# Test para estructura 'for'
class CICLOS:
    numeros = [1, 2, 3]
    for n in numeros:
        print(n)  # Dentro del bloque, no se cuenta como lógica

    letras = ['a', 'b', 'c']
    while True:
        print(letras)
